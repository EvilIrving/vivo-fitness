<template>
  <div class="flex flex-col rounded-lg bg-white p-4 shadow-sm">
    <div class="flex items-center justify-between">
      <div class="flex-1">
        <text class="text-2xl font-bold text-gray-800">{{ actionName }}</text>
        <text class="mt-1 text-xl text-gray-500">{{ date }}</text>
      </div>
      <div class="flex flex-col items-end">
        <text class="text-4xl font-bold text-brand">{{ totalReps }}</text>
        <text class="mt-1 text-lg text-gray-400">次</text>
      </div>
    </div>
    <div class="mt-3 flex items-center justify-between border-t border-gray-100 pt-3">
      <text class="text-xl text-gray-400">{{ setsCount }} 组</text>
      <text class="text-xl text-gray-400" if="{{ duration }}">{{ duration }}秒</text>
    </div>
  </div>
</template>

<script>
  export default {
    props: {
      actionName: {
        default: '',
      },
      date: {
        default: '',
      },
      totalReps: {
        default: 0,
      },
      setsCount: {
        default: 0,
      },
      duration: {
        default: 0,
      },
    },
  }
</script>

<style>
  @tailwind utilities;
</style>
