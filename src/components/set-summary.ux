<template>
  <div class="flex flex-col rounded-lg bg-gray-50 p-4">
    <text class="text-2xl text-gray-600">第 {{ setNumber }} 组</text>
    <div class="mt-3 flex items-baseline justify-between">
      <text class="text-5xl font-bold text-brand">{{ actualReps }}</text>
      <text class="text-2xl text-gray-400">/ {{ targetReps }} 次</text>
    </div>
    <text class="mt-2 text-lg text-gray-400" if="{{ duration }}">用时: {{ duration }}秒</text>
  </div>
</template>

<script>
  export default {
    props: {
      setNumber: {
        default: 1,
      },
      actualReps: {
        default: 0,
      },
      targetReps: {
        default: 12,
      },
      duration: {
        default: 0,
      },
    },
  }
</script>

<style>
  @tailwind utilities;
</style>
