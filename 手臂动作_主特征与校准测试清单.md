# 手臂动作 — 主特征维度、校准验证步骤与真机测试清单

概述

- 目标：为首批「手臂相关动作」制定主观测特征、校准（个体模板）步骤和可执行的真机测试清单，便于在 Apple Watch 上快速验证与迭代。
- 首批建议动作（优先级）：引体向上 (ACT_PULLUP)、仰卧起坐 (ACT_CRUNCH)、哑铃弯举 (ACT_BICEP)、杠铃卧推/器械夹胸 (ACT_BENCH/ACT_CHEST)。建议先以 3 个为 MVP。

一、通用说明（适用于所有动作）

- 佩戴与环境
  - 佩戴侧固定：校准与训练期间保持左或右腕一致（保存 `wearSide`）。
  - 表冠方向：建议统一（例如表冠朝外/上），并在校准提示中要求用户按文案摆放。
  - 表带：中等紧度，避免松动滑动。
- 传感器维度优先级（建议在实现里配置）
  - gravityAlongAxis：重力在某轴的投影（姿态代理）。
  - fusedPitch / PitchProxy：俯仰角代理（结合 gravity 与 accel）。
  - rotationRate（gyro）：角速度（方向反转检测）。
  - accEnvelope：加速度包络/模长（检测用力段）。
  - integratedVelocity / shortDisplacement：短周期积分（仅作辅助 OR 条件）。
- 校准通用流程（模板生成）
  - 佩戴检查提示（静止 2s）——记录基线姿态（A 区初值）。
  - 引导重复：完成 N 次（默认 8，范围 5–10，可配置）连续标准动作，记录全程传感器流。
  - 结束静止 2s——核对回到 A 区。
  - 特征抽取：针对主监测轴提取每次重复的关键标量（B 区峰值、A/B 分位边界、行程幅度、持续时间）。
  - 构建 MotionTemplate：zoneA、zoneB、minStroke、minIntervalMs（防抖）、maxRepDurationMs、primaryFeature、wearSide、calibratedAt。
  - 验证准入：若校准段无法分离出稳定 A/B（边界重叠或方差过大），判为失败并提示重试或更换佩戴/动作。

二、动作级建议（主特征与校准要点）

## 引体向上 (Pull-up)

- 观测要点：手腕随身体垂直位移产生显著俯仰与重力投影变化；通过顶点处常见角速度方向反转。
- 推荐主特征：pitchProxy（或 gravityAlongY）为主，gyroY 辅助。
- 校准要点：
  - 起始静止：悬挂低位 2s（A 区）。
  - 完成 5–8 次标准引体向上（到高点再回低点）。
  - 由样本计算 zoneA（基线区）、zoneB（高位分位区，例如 75–95% 峰值区间）。
  - minStroke：B 区峰值 − A 区中位 ≥ 噪声带宽 * 常数。
- 验收：校准样本中至少 5 次出现稳定 A→B→A 闭环，峰值与基线差异明显。

## 仰卧起坐 / 卷腹 (Crunch / Sit-up)

- 观测要点：手腕相对重力方向在起身时发生俯仰/前倾变化，姿势差异大（抱胸/手臂位置影响显著）。
- 推荐主特征：gravityAlongX 或 pitchProxy 为主，accZ 包络做辅助。
- 校准要点：
  - 明确姿势约束（如抱胸或手放胸前），记录该姿势基线。
  - 完成 8 次动作（可分别做慢速/正常/快速以测鲁棒性）。
  - 若 A/B 不可分离，提示用户调整姿势或换动作。
- 验收：主特征在 ≥5 次中能观测到稳定双区间穿越。

## 哑铃弯举 (Dumbbell Biceps Curl)

- 观测要点：前臂/手腕旋转与角位移明显，角速度在弯举/回落时方向翻转。
- 推荐主特征：gyroY（角速度）或 pitchProxy，accEnvelope 用于检测用力段。
- 校准要点：
  - 起始静止：手臂伸直低位 2s；完成 6–8 次弯举。
  - 记录峰值角速度与高位停顿特征（B 区）。
  - 建议 minIntervalMs ≥ 300ms，maxRepDurationMs 设为宽值（如 8–12s）。
- 验收：每次重复表现出明显角速度翻转并回到基线。

## 杠铃卧推 / 器械夹胸（候选）

- 观测要点：水平推举在手腕上表现为水平方向的姿态/投影微变，观测轴依握法/表冠方向而定。
- 推荐主特征：gravityAlongX / accEnvelope 结合 pitchProxy。
- 校准要点：固定握法与表冠方向，完成 6–8 次示范动作；若无明显双区间则不入库。

三、真机测试清单（分层、可量化）

A. 数据采集与标注（基础）

- 设备与环境
  - Apple Watch Series 10（或目标机型），配对 iPhone，打开 Xcode 日志与开发者模式。
  - 采样率目标 50 Hz，记录时间同步策略（UTC ms）。
- 参与者与样本
  - 初轮人数：8–12 人（覆盖不同臂长/体型/性别）。
  - 每人每动作至少 3 组：慢速、正常、快速（每组 8–12 次或到力竭）。
  - 若支持左右手，需采集左右手样本。
- 采样协议
  - 校准段：静止 2s → N 次演示（5–10）→ 静止 2s（保存原始流）。
  - 标注：视频同步标注每次起点/顶点时间戳（Ground Truth），录像与手表时间对齐。

B. 算法功能验证（真机）

- 校准成功率
  - 指标：校准段能生成有效模板的比率（目标 ≥ 90%）。
- 单次计数准确率
  - 指标：召回率 TP/(TP+FN)（漏计率 FN）、误报率 FP/总检测。初始目标：漏计 ≤ 10%，误计 ≤ 10%。
- 重复稳定性（鲁棒性）
  - 指标：在力竭/慢速阶段准确率下降幅度（期望 < 20%）。
- 防抖与去重效果
  - 指标：微抖导致的重复计数率（应 ≈ 0）。
- 延迟与实时性
  - 指标：从完成动作到计数+触觉通知延迟（目标 < 200 ms）。

C. 交互与体验验证

- 震动体验：短震（每次计数）与长震（达标）主观可感知且无明显延迟。
- 暂停/继续：暂停后传感器停止采样并可靠恢复；恢复后不丢失计数上下文。
- 校准失败提示：向用户给出明确可操作建议（如“表带偏松/换手/重试”）。

D. 功耗与稳定性

- 电量：在 50 Hz 下模拟 30 分钟间歇训练，记录电量变化与处理开销估算。
- 存储：长时间记录后本地存储增长与回放性能（例如 100 次训练后的读取）。

E. 边界与异常测试

- 非训练动作干扰：调整器械、甩手、短暂停顿等不应被误计（检验误报率）。
- 换握法/换手：若行为改变导致模板失效，应提示重新校准。
- 顶点长时间停留：状态机对长停留应避免重复计数并在超时后重置。

四、可交付清单（验收项）

- 为每个首批动作保存 MotionTemplate 示例（包含 zoneA/zoneB/minStroke/minInterval 等）。
- 在真实手表上完成 ≥8 人采集的测试包，且每个动作满足：校准成功率 ≥90%，计数漏计 ≤10%、误计 ≤10%（首轮目标）。
- 触觉延迟 <200 ms，暂停/继续稳定。
- 输出一份测试包：样例原始传感器流（校准段+训练段）及对应标注（起点/顶点时间戳）。

五、快速落地建议

- 首轮只实现 3 个动作（Pull-up / Crunch / Biceps Curl），在 4–8 人真实设备上做快速验证并调整阈值。
- 优先实现：模板化校准 + 宽容状态机计数；组内自适应作为次轮功能。
- 将主特征与阈值配置化（actionCode → primaryFeature），便于后续扩展无需改大量代码。

附：示例 MotionTemplate 字段（参考 docs/01）

```yaml
MotionTemplate:
  actionCode: "ACT_BICEP"
  primaryFeature: "gyroY"
  zoneA: { min: -0.12, max: 0.05 }
  zoneB: { min: 0.6, max: 1.2 }
  minStroke: 0.5
  minIntervalMs: 300
  maxRepDurationMs: 12000
  calibratedAt: 1680000000
  wearSide: "rightWrist"
```

---

文件维护：将此文档作为首轮真机验证的活文档，实测中若调整轴或阈值，请同步回 docs/01 并记录变更理由。